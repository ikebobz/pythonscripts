# -*- coding: utf-8 -*-
"""
Spyder Editor

This is a temporary script file.
"""

import psycopg

'''with psycopg.connect("dbname = lp_badagry user=agent1 password=lamis") as conn:
    with conn.cursor() as cur:
        cur.execute("select push_to_server()")
        cur.fetchone()
        for record in cur:
            print(record)
            conn.commit()'''
def add(a,b) -> int:
    return a + b;
    